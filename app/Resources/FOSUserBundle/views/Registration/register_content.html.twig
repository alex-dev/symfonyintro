{% trans_default_domain 'FOSUserBundle' %}

{{- form_start(form, {
  'method': 'post',
  'action': path('fos_user_registration_register'),
  'attr': {'class': 'fos_user_registration_register container-fluid', 'novalidate': 'novalidate'}
}) -}}
  {{- form_widget(form) -}}
  <div class="text-center">
    <button type="submit" class="btn btn-template-outlined">
      {%- trans -%}
        registration.submit
      {%- endtrans -%}
    </button>
  </div>
{{- form_end(form) -}}
