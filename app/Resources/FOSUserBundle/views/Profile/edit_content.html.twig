{% trans_default_domain 'FOSUserBundle' %}

{{- form_start(form, {
  'method': 'post',
  'action': path('fos_user_profile_edit'),
  'attr': {'class': 'fos_user_profile_edit container-fluid', 'novalidate': 'novalidate'}
}) -}}
  {{- form_widget(form) -}}
  <div class="text-center">
    <button type="submit" class="btn btn-template-outlined">
      {%- trans -%}
        profile.edit.submit
      {%- endtrans -%}
    </button>
  </div>
{{- form_end(form) -}}
